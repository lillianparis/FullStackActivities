// Here is an && Operator
// This just allows us to shove two tests into one condition

let age= 20;
let ownsCar= true;

if (age >= 16 && ownsCar) {
  console.log("Tom Nook is old enough to drive");
} else {
  console.log("Tom Nook does not drive.");
}
